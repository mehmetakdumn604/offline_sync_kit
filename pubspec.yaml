name: offline_sync_kit
description: A comprehensive Flutter package for offline data synchronization with remote APIs, featuring real-time WebSocket support, automatic conflict resolution, and customizable options.
version: 1.4.0
homepage: https://github.com/mehmetakdumn604/offline_sync_kit

environment:
  sdk: ^3.0.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  uuid:
  http:
  connectivity_plus: ^6.1.3
  path_provider:
  sqflite:
  equatable: ^2.0.7
  intl:
  rxdart:
  path:
  encrypt: ^5.0.3
  crypto:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  isar_generator: ^3.1.0+1
  build_runner: ^2.4.8

flutter:
  # To add assets to your package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # To add custom fonts to your package, add a fonts section here:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
